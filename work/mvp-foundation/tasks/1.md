---
status: planned
depends_on: []
wave: 1
skills: [infrastructure-setup]
verify: [smoke]
reviewers: [infrastructure-reviewer, test-reviewer]
teammate_name: foundation-tooling
---

# Task 1: Python package and test tooling

## Required Skills

Before executing the task, load:
- `/skill:infrastructure-setup` — [skills/infrastructure-setup/SKILL.md](~/.claude/skills/infrastructure-setup/SKILL.md)

## Description

Create Python project metadata, package initialization, and a reliable local test command. This enables all later foundation work to be verified consistently.

## What to do

- Add Python project metadata for the MVP foundation.
- Make `app/` importable as a package.
- Add minimal package init files where needed.
- Configure pytest discovery.
- Add a minimal smoke test proving the test command works.

## TDD Anchor

Tests to write BEFORE implementation. Write → run → confirm they fail → write code → confirm they pass.

- `tests/test_project_tooling.py::test_app_package_imports` — importing `app` succeeds.
- `tests/test_project_tooling.py::test_pytest_smoke` — basic test infrastructure is active.

## Acceptance Criteria

- [ ] `python -m pytest` runs from repository root.
- [ ] `app` imports as a Python package.
- [ ] Test discovery works without production secrets.
- [ ] No Docker, PostgreSQL, Redis, Celery, or Kubernetes tooling is added.

## Context Files

- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [README.md](../../README.md)
- [.gitignore](../../.gitignore)
- [.env.example](../../.env.example)

## Verification Steps

### Automated
- `python -m pytest -v` → tests are discovered and pass.

### Smoke
- `python -c "import app; print('ok')"` → prints `ok`.

## Details

**Files:** `pyproject.toml` — project metadata and pytest config.  
**Files:** `app/__init__.py` and package `__init__.py` files — make packages importable.  
**Files:** `tests/test_project_tooling.py` — initial smoke tests.

**Dependencies:** None.

**Edge cases:** Test command must not require `.env`, Telegram tokens, Google credentials, or network access.

## Reviewers

- **infrastructure-reviewer** → `work/mvp-foundation/logs/working/task-1/infrastructure-reviewer-{round}.json`
- **test-reviewer** → `work/mvp-foundation/logs/working/task-1/test-reviewer-{round}.json`

## Post-completion

- [ ] Write a brief report in decisions.md per the template.
- [ ] If you deviated from the spec — describe the deviation and the reason.
- [ ] Update user-spec/tech-spec if anything changed.

